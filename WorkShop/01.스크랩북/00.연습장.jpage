/*****************************************************//*****************************************************/net.dstone.common.utils.DateUtil.stopWatchStart("00.연습장");String line = "";String charSet = "EUC-KR";try {	/*	String[] lines = net.dstone.common.utils.FileUtil.readFileByLines("C:/Temp/임시/ABRQ20250625_80450088300.001.171344", charSet);	for(int i=0; i<lines.length; i++){		line = lines[i];		if(line.equals("")){			continue;		}				if( i == 1 ){			int offset = 0;			int len = 1;			System.out.println("line["+line+"]-len("+line.getBytes(charSet).length+")");						String word = net.dstone.common.utils.StringUtil.subByteString(line, offset, (offset = offset+len) );			line = net.dstone.common.utils.StringUtil.subByteString(line, len );			System.out.println("word["+word+"]-len("+word.getBytes(charSet).length+")" + " line["+line+"]-len("+line.getBytes(charSet).length+")");					}	}	*/		String base = "";	base = net.dstone.common.utils.StringUtil.appendFld(base, "D", 1, charSet); // Record 구분	base = net.dstone.common.utils.StringUtil.appendFld(base, "020", 3, charSet); // 은행코드	base = net.dstone.common.utils.StringUtil.appendFld(base, "1002260905569", 15, charSet); // 고객계좌번호	base = net.dstone.common.utils.StringUtil.appendFld(base, "00000005928", 11, charSet); // 이체금액	base = net.dstone.common.utils.StringUtil.appendFld(base, "", 1, charSet); // 이체결과	base = net.dstone.common.utils.StringUtil.appendFld(base, "", 4, charSet); // 불능코드	base = net.dstone.common.utils.StringUtil.appendFld(base, "하하하하하하하하하하하하핳", 12, charSet); // 통장인자내역	base = net.dstone.common.utils.StringUtil.appendFld(base, "", 31, charSet); // 고객영역	base = net.dstone.common.utils.StringUtil.appendFld(base, "", 2, charSet); // 공란	System.out.println("["+base+"]-len["+base.getBytes(charSet).length+"]");		int offSet = 0;	int len = 0;	System.out.println("Record 구분 [" + net.dstone.common.utils.StringUtil.substrFld(base, (offSet=offSet+len), (len = 1), charSet) + "]" );	System.out.println("은행코드 [" + net.dstone.common.utils.StringUtil.substrFld(base, (offSet=offSet+len), (len = 3), charSet) + "]" );	System.out.println("고객계좌번호 [" + net.dstone.common.utils.StringUtil.substrFld(base, (offSet=offSet+len), (len = 15), charSet) + "]" );	System.out.println("이체금액 [" + net.dstone.common.utils.StringUtil.substrFld(base, (offSet=offSet+len), (len = 11), charSet) + "]" );	System.out.println("이체결과 [" + net.dstone.common.utils.StringUtil.substrFld(base, (offSet=offSet+len), (len = 1), charSet) + "]" );	System.out.println("불능코드 [" + net.dstone.common.utils.StringUtil.substrFld(base, (offSet=offSet+len), (len = 4), charSet) + "]" );	System.out.println("통장인자내역 [" + net.dstone.common.utils.StringUtil.substrFld(base, (offSet=offSet+len), (len = 12), charSet) + "]" );	System.out.println("고객영역 [" + net.dstone.common.utils.StringUtil.substrFld(base, (offSet=offSet+len), (len = 31), charSet) + "]" );	System.out.println("공란 [" + net.dstone.common.utils.StringUtil.substrFld(base, (offSet=offSet+len), (len = 2), charSet) + "]" );		} catch (Exception e) {	e.printStackTrace();}finally{	net.dstone.common.utils.DateUtil.stopWatchEnd("00.연습장");}