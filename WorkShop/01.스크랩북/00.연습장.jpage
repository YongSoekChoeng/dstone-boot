/*****************************************************//*****************************************************/net.dstone.common.utils.DateUtil.stopWatchStart("00.연습장");try {	String[] sData = null;	int totNum = 37;	int numMaps = 5;		sData = new String[totNum];	for(int i=0; i<totNum; i++){		sData[i] = String.valueOf(i);	}		String[][] splits = net.dstone.common.utils.StringUtil.splitStringArray(sData, numMaps);	if(splits != null){		for(int i=0; i<splits.length; i++){			String[] subArr = splits[i];			System.out.println("subArr.length["+subArr.length+"]");			for(String subStr : subArr){				System.out.println("  subStr["+subStr+"]");			}		}	}	} catch (Exception e) {	e.printStackTrace();}finally{	net.dstone.common.utils.DateUtil.stopWatchEnd("00.연습장");}