/*****************************************************//*****************************************************/net.dstone.common.utils.DateUtil.stopWatchStart("00.웹호출");try {		String plainStr = "D:/AppHome/dstone-boot/ssl/dstone-keystore.p12";    String encStr = net.dstone.common.utils.EncUtil.encrypt(plainStr);        System.out.println(encStr);        String decStr = net.dstone.common.utils.EncUtil.decrypt(encStr);    System.out.println(decStr);    } catch (Exception e) {	e.printStackTrace();} finally{	net.dstone.common.utils.DateUtil.stopWatchEnd("00.웹호출");}